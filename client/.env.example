# Leave empty when the frontend is served behind the same domain/proxy as the API.
# Set this for split deployments, for example:
# VITE_API_URL="https://api.example.com"
VITE_API_URL="http://localhost:4000"
